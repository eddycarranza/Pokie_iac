# Colecciones necesarias. Instalar con:
#   ansible-galaxy collection install -r requirements.yml -p ./collections
collections:
  - name: amazon.aws
    version: ">=7.0.0"
  - name: community.aws
    version: ">=7.0.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.postgresql
    version: ">=3.0.0"
